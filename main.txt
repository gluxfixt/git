#include <stdio.h>
#include <string.h>

int main(int argc, char **argv)
{
    char name[256];

    printf("Enter your name: ");
    fgets(name, 256, stdin);
    printf("length = %d\n", strlen(name));
    name[strlen(name)-1] = '\0';

    printf("Hello world!\n");
    return 0;
}
